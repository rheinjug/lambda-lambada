package aufwaermen;

public class Filter {

    /**
     * Diese Funktion soll einen Stream von 1 bis einschließlich n generieren und nur die gerade Zahlen herausfiltern.
     * Verwende Streams und die Funktion `filter`, welche ein Prädikat annimmt (auch als Lambda), und damit entscheidet,
     * ob die Zahl gefiltert wird oder nicht.
     * <p>
     * Zurückgegeben werden soll die Summe der nun gerade Zahlen.
     * <p>
     * Es gibt ein paar Tests.
     *
     * @param n
     * @return Summe gerader Zahlen
     */
    int summeGeradeZahlen(int n) {
        return -1;
    }

}
